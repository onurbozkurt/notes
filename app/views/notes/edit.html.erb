<div class="flex justify-between">
  <h1 class="font-bold text-4xl mb-4"><%= @note.title %></h1>
  <%= link_to "Delete", note_path(@note), data: { "turbo-method": :delete, turbo_confirm: "Are you sure?" }, class: "font-bold text-red-500" %>
</div>

<%= render "form" %>
