<h1 class="font-bold text-4xl mb-4">Notes</h1>
<div class="flex flex-col gap-2">
  <%= link_to "+ Add", new_note_path, class: "font-bold" %>
  <% @notes.each do |note| %>
    <%= render note %>
  <% end %>
</div>
